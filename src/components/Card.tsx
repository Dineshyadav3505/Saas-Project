import Link from 'next/link';
import Image from 'next/image';
import React from 'react';

interface CardProps {
  img: string; // Expecting a string for the image path
}

const Card: React.FC<CardProps> = ({ img }) => {
  return (
    
  );
};

export default Card;